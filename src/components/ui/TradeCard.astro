---
import Button from "./Button.astro";

interface Props {
    name: string;
    slug: string;
    image: string;
    templateCount: number;
}

const { name, slug, image, templateCount } = Astro.props;
---

<a
    href={`/our-work#${slug}`}
    class="group relative overflow-hidden bg-white shadow-lg hover:shadow-2xl transition-all duration-300"
>
    <div class="relative h-64 overflow-hidden">
        <img
            src={image}
            alt={`${name} - Professional website templates and services`}
            class="w-full h-full object-cover group-hover:scale-110 transition-transform duration-500"
        />
        <div
            class="absolute inset-0 bg-linear-to-t from-black/70 to-transparent"
        >
        </div>
        <div class="absolute bottom-0 left-0 right-0 p-6">
            <h3
                class="text-xl md:text-2xl font-heading font-bold text-white mb-2"
            >
                {name}
            </h3>
            <p class="text-base text-gray-200 font-body">
                {templateCount}
                {templateCount === 1 ? "Template" : "Templates"} Available
            </p>
        </div>
    </div>
    <div class="p-6 bg-white">
        <Button
            variant="accent"
            size="md"
            icon={false}
            class="w-full text-center"
            aria-label={`View ${name} templates`}
        >
            View Templates
        </Button>
    </div>
</a>
