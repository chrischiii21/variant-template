---
import Button from "../ui/Button.astro";

interface Props {
    title?: string;
    subtitle?: string;
    primaryCtaText?: string;
    primaryCtaLink?: string;
    secondaryCtaText?: string;
    secondaryCtaLink?: string;
    backgroundColor?: string;
}

const {
    title = "Ready to Grow Your Business?",
    subtitle = "Join hundreds of contractors who are getting more customers with our professional templates",
    primaryCtaText = "Browse All Templates",
    primaryCtaLink = "/our-work",
    secondaryCtaText = "Contact Sales",
    secondaryCtaLink = "/contact",
    backgroundColor = "var(--color-primary)",
} = Astro.props;
---

<section class="py-16 px-6" style={`background-color: ${backgroundColor};`}>
    <div class="mx-auto text-center">
        <h2 class="heading-h2 font-heading font-bold text-white mb-4">
            {title}
        </h2>
        <p class="text-xl font-body text-gray-200 mb-12">
            {subtitle}
        </p>
        <div class="flex flex-col sm:flex-row gap-4 justify-center">
            <Button
                variant="accent"
                size="md"
                href={primaryCtaLink}
                icon={false}
            >
                {primaryCtaText}
            </Button>
            <Button
                variant="outline"
                size="md"
                href={secondaryCtaLink}
                icon={false}
                class="!text-white !border-white hover:!bg-white hover:!text-[var(--color-primary)]"
            >
                {secondaryCtaText}
            </Button>
        </div>
    </div>
</section>
